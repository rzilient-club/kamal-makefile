# Install 1Password client: https://developer.1password.com/docs/cli/get-started/
# Or install Omakub https://github.com/basecamp/omakub which includes both 1Password and op client.

<%  session_token = `op signin --account rzilientgroup.1password.com --raw` %>

# Generated by kamal envify
<% if (user = `op whoami`) != ""%>
  KAMAL_REGISTRY_PASSWORD=<%= `op read "op://your_shared_space/KAMAL_REGISTRY_PASSWORD" -n` %>
  RAILS_MASTER_KEY=<%= `op read "op:/your_shared_space/RAILS_MASTER_KEY" -n` %>
<% else raise ArgumentError, "Session token missing" end %>
